<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Login Form</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- MATERIAL DESIGN ICONIC FONT -->
    <link rel="stylesheet" href="{{ asset('fonts/material-design-iconic-font/css/material-design-iconic-font.min.css') }}">
    <!-- STYLE CSS -->
    <link rel="stylesheet" href="{{ asset('css/styleh.css') }}">
    <style>
        /* Ajouter une marge autour du formulaire */
        .login-form {
            margin: 50px;
        }

        /* Changer la couleur des liens en beige */
        .forgot-pass a,
        .signup a {
            color:#d6d0b8	;
        }

         .signup {
            margin-top: 10px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
<div class="wrapper" style="background-image: url('{{ asset('img/output-onlinepngtools.png') }}');">
    <div class="inner">
        <div class="image-holder">
            <img src="{{ asset('img/1.jpg') }}" height="650px">
        </div>

        <!-- Intégration du formulaire Symfony ici -->
       <form method="post"  class="login-form"   {{ form_start(resetForm) }}>
        <!-- {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %} -->

       <h3>Reset your password</h3>
<div >
    {{ form_row(resetForm.plainPassword.first, {'attr': {'class': 'form-control', 'placeholder': 'Enter your new password'}}) }}
</div>

<div >
    {{ form_row(resetForm.plainPassword.second, {'attr': {'class': 'form-control', 'placeholder': 'Confirm your new password'}}) }}
</div>

                <button type="submit" class="btn btn-primary">Reset Password</button>

                       {{ form_end(resetForm) }}

</form>
        <!-- Fin du formulaire Symfony -->
    </div>
</div>
</body>
</html>